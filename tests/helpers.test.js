import {
  paletteColorBuilder,
  adjustColor,
  calculateColor
} from '../src/utils/helpers'

describe('Helper functions', () => {
  describe('paletteColorBuilder function', () => {
    test('test', () => {
      //
    })
  })
})